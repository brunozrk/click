<% unless flash.empty? %>
<div class="content">
  <% flash.each do |type, msg| %>
    <div class="alert alert-<%= flash_class(type) %> alert-dismissable">
      <i class="fa <%= flash_icon(type) %>"></i>
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
      <%= msg %>
    </div>
  <% end %>
</div>
<% end %>
